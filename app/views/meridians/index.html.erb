<h1>Listing meridians</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @meridians.each do |meridian| %>
  <tr>
    <td><%= meridian.name %></td>
    <td><%= link_to 'Show', meridian %></td>
    <td><%= link_to 'Edit', edit_meridian_path(meridian) %></td>
    <td><%= link_to 'Destroy', meridian, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Meridian', new_meridian_path %>
