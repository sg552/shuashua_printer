<%= form_for(@meridian) do |f| %>
  <% if @meridian.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meridian.errors.count, "error") %> prohibited this meridian from being saved:</h2>

      <ul>
      <% @meridian.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
