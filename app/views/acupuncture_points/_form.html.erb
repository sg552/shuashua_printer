<%= form_for(@acupuncture_point) do |f| %>
  <% if @acupuncture_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@acupuncture_point.errors.count, "error") %> prohibited this acupuncture_point from being saved:</h2>

      <ul>
      <% @acupuncture_point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :index %><br />
    <%= f.number_field :index %>
  </div>
  <div class="field">
    <%= f.label :meridian_id %><br />
    <%= f.number_field :meridian_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
