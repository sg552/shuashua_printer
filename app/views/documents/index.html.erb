<h1>文档列表</h1>

<table>
  <tr>
    <th>名字</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @documents.each do |document| %>
  <tr>
    <td><%= document.user_name %></td>
    <td><%= link_to '编辑', edit_document_path(document) %></td>
    <td><%= link_to '删除', document, :confirm => '确定吗？', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to '新建一个文档', new_document_path %>
